const PrivacyNote = () => {
  return (
    <footer className="bg-green-50 text-center py-8 px-4">
      <p className="text-gray-700 text-sm leading-relaxed max-w-2xl mx-auto">
        🔒 Your Email and Mobile are required for validation but will never be shared publicly.
        Information is used only for verification and engagement for climate action initiatives.
      </p>
    </footer>
  );
};

export default PrivacyNote;
